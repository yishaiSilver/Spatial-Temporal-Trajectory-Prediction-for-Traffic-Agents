# config.yaml
data:
  train_path: ./data/train
  val_path: ./data/val_in
  batch_size: 1
  shuffle: true
  num_workers: 4
  transforms:
    - AgentCenter
  collate:
    - p_in
    # - v_in
    # - lane

